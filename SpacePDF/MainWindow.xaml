<Window x:Class="SpacePDF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SpacePDF" Height="340" Width="540"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Border Width="440" Height="220" Background="White" CornerRadius="12"
                BorderBrush="#E0E0E0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.12"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="SpacePDF" FontSize="28" FontWeight="Bold"
                           Foreground="#1E293B" HorizontalAlignment="Center"
                           Margin="0,0,0,4"/>
                <TextBlock Text="PDF Whitespace Editor" FontSize="14"
                           Foreground="#94A3B8" HorizontalAlignment="Center"
                           Margin="0,0,0,24"/>

                <Button x:Name="SelectPdfButton" Content="Select PDF File"
                        Click="SelectPdfButton_Click"
                        Width="200" Height="40" FontSize="14"
                        Background="#3B82F6" Foreground="White"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock x:Name="FilePathText" Text="No file selected"
                           FontSize="12" Foreground="#94A3B8"
                           HorizontalAlignment="Center" Margin="0,12,0,0"
                           TextTrimming="CharacterEllipsis" MaxWidth="380"/>

                <TextBlock x:Name="VersionLabel" Text=""
                           FontSize="12" Foreground="#64748B"
                           HorizontalAlignment="Center" Margin="0,6,0,0"/>

                <Button x:Name="OpenPreviewButton" Content="Open Preview"
                        Click="OpenPreviewButton_Click"
                        Width="200" Height="36" FontSize="13"
                        Margin="0,16,0,0"
                        Background="#10B981" Foreground="White"
                        BorderThickness="0" IsEnabled="False">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
