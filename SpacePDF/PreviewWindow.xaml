<Window x:Class="SpacePDF.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SpacePDF"
        Title="SpacePDF - Preview" Height="800" Width="1100"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized"
        Background="{StaticResource BackgroundBrush}">


    <Grid>
        <ScrollViewer x:Name="MainScrollViewer">
            <StackPanel x:Name="PagesContainer" HorizontalAlignment="Center" VerticalAlignment="Top">
                <StackPanel.LayoutTransform>
                    <ScaleTransform x:Name="PageScaleTransform" ScaleX="1.0" ScaleY="1.0" />
                </StackPanel.LayoutTransform>

                <!-- 动态添加的页面 Image 将放置在这里 -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
